package com.ruslanito.Core_General;

/**
 * Created by Ruslanito on 05.02.2017.
 */
public class Queue2 {
    private char q[];
    private int putloc, getloc;

  /*  Queue2(int size) {
        Queue2(Queue2 ob) {
            putloc = ob.putloc;
            getloc = ob.getloc;
            q = new char[ob.q.length];

            for (int i = getloc + 1; i <= putloc; i++)
                q[i] = ob.q[i];
        }
        //Конструктор очереди на основе исходных значений
        Queue2( char a[]){
            putloc = 0;
            getloc = 0;
            q = new char[a.length + 1];
            for (int i = 0; i < a.length; i++) ;
        }
    }

        //Поместить символ в очередь
        void put (char ch){

    }
    }
    */
}
